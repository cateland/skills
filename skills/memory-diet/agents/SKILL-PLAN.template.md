# Skill-plan dossier: `<suggested-name>`

> Evidence and intent for a later skill-writing session. The skill writer decides the resulting skill's invocation, interface, leading words, steps, completion criteria, and disclosure structure.

## Capture

| Field | Value |
|---|---|
| Status | Proposed |
| Captured on | `<YYYY-MM-DD>` |
| Repository revision | `<commit SHA, no commits, or unavailable>` |
| Working-tree state | `<clean, modified, or unavailable>` |
| Suggested skill name | `<name>` |

## Candidate purpose

`<What recurring capability should replace this conditional memory, and why does it not belong in every session?>`

## Source evidence

Repeat this section for every source excerpt included in the candidate.

### `<path>` — `<line range at capture>`

- **Directory scope:** `<scope>`
- **Source state:** `<revision, modified, untracked, or unavailable>`
- **Diet verdict:** `EXTRACT`
- **Why extracted:** `<why this is useful but conditional or domain-specific>`

Exact excerpt:

```md
<verbatim instruction text>
```

## Relevant history

Record only history that helps recover intent. State `No relevant history found` or `Git history unavailable` when applicable.

| Revision | Change | Why it matters |
|---|---|---|
| `<SHA/date>` | `<what changed>` | `<intent or constraint revealed>` |

## Repository context

- **Related source/configuration/docs:** `<paths and what they establish>`
- **Situations that need this guidance:** `<observed triggers>`
- **Terms or domain facts:** `<facts a later skill writer cannot infer safely>`

## Intended outcomes and evidence

- **Desired outcome:** `<observable result implied by the source>`
- **Known failure or example:** `<concrete evidence, or "None found">`
- **Human preference or safety boundary:** `<if present>`

## Constraints and dependencies

- `<environment, tooling, related skills, or hard boundaries>`

## Activation replacement

- **Target reference:** `<document or source the replacement must load>`
- **Replacement mechanism:** `<installed skill, imported memory, hook, enforced command, or none yet>`
- **Activation trigger:** `<ordinary relevant task step that loads the target before the affected decision>`
- **Activation evidence:** `<representative task validation, deterministic mechanism, or none>`
- **Removal readiness:** `<ELIGIBLE or BLOCKED>`

## Open questions

- `<question that source and history could not resolve, or "None">`

## Memory disposition

- **Planned source edit:** `<remove, replace, or retain pending an active replacement>`
- **Approval state:** `<pending, approved, rejected, or blocked>`
- **Applied in revision:** `<SHA, uncommitted, not applied, or unavailable>`
