# Repository guidance

Keep every skill harness-neutral and compliant with the Agent Skills standard.

Place a skill's progressive-disclosure documents, including templates, in that skill's `agents/` directory. Reference them with paths relative to `SKILL.md`.

Keep shared repository guidance here rather than duplicating it in harness-specific instruction files.
